{
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --outfile=dist/out.js",
    "tsc": "tsc"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "husky": "^9.1.6",
    "typescript": "^5.6.3"
  }
}
